---
const { innerText, fill, border } = Astro.props;
---

<div
  id="button"
  class={`bg-${fill} px-4 py-2 flex justify-center ${border} w-fit mx-auto sm:mx-0 sm:self-start`}
>
  <p class="text-base-950">{innerText}</p>
</div>
