---
const { hoverFill, innerText, fill, border } = Astro.props;
---

<div
  id="button"
  class={`active:scale-95 hover:bg-pinky-200 bg-${fill} hover:bg-${hoverFill} rounded-sm px-4 py-2 flex justify-center ${border} w-fit sm:mx-0 sm:self-start`}
>
  <p class="text-base-950">{innerText}</p>
</div>
